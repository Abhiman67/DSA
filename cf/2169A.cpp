#include<iostream/stdc++.h>
using namespace std;

int main(){

    int t;
    cin >> t;

    while(t--){
        
        int n, a;
// n -> nu of marbles in bag and a is  nu choosen by alex

        cin >> n >> a;

        vector<int>nums;
        for(int i=0;i<nums.size();i++){

            cin >> nums[i];
        }

        for(int i=0;i<n;i++){

            if(nums[i]-a)
        }

    }
}